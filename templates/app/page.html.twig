{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12 admin-page">
                <h1>{{ page.title(app.request.locale) }}</h1>
                {{ page.description(app.request.locale)|raw }}
            </div>
        </div>
    </div>
{% endblock %}